[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
markers = [
    "db: hits the real Supabase database inside a rolled-back transaction (skipped without DATABASE_URL)",
]
